apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: ld-rr
  labels:
    name: auth-service
spec:
  type: NodePort
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
    nodePort: 30007
  selector:
    app: auth-service
    tier: backend
    environment: development
