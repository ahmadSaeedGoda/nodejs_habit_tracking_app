apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: auth-postgres-pvc
  namespace: ld-rr
  labels:
    app: auth-postgres-pvc
    tier: backend
    environment: development
    role: database
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: "256Mi"
